

    <nav class="navbar navbar-default">
      <div class="container">
        <a href='/' class='navbar-brand'>Twitter-Curious</a>
          <ul class='nav navbar-nav active dropdown pull-right dropdown-toggle' data-toggle="dropdown">
            <% if current_user %>
              <li><p>logged in as: <%= current_user.name %></p></li>
              <%= link_to "Logout", logout_path %>
            <% else %>
              <li><%= link_to "Login", login_path %></li>
            <% end %>
          </ul>
      </div>
    </nav>
